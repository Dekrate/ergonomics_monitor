spring:
  application:
    name: ergonomics-monitor
  threads:
    virtual:
      enabled: true
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/ergonomics_db
    username: postgres
    password: password
  datasource:
    url: jdbc:postgresql://localhost:5432/ergonomics_db
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true
  ai:
    ollama:
      base-url: http://localhost:11434
      chat:
        model: SpeakLeash/bielik-11b-v3.0-instruct:bf16
        options:
          temperature: 0.7
          max-tokens: 500

server:
  port: 8081

ergonomics:
  break-check:
    cron: "0 */15 * * * *"  # Co 15 minut
  notifications:
    windows:
      enabled: true
    log-only:
      enabled: false

logging:
  level:
    pl.dekrate.ergonomicsmonitor: DEBUG
    org.springframework.r2dbc: DEBUG
    org.springframework.ai: DEBUG
    org.springframework.web.client: DEBUG
    org.springframework.web.reactive.function.client: DEBUG
    reactor.netty.http.client: DEBUG
